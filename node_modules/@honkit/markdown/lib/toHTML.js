"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const markup_it_1 = __importDefault(require("@honkit/markup-it"));
const markdown_1 = __importDefault(require("@honkit/markup-it/syntaxes/markdown"));
const html_1 = __importDefault(require("@honkit/markup-it/syntaxes/html"));
const markdown = new markup_it_1.default(markdown_1.default);
const html = new markup_it_1.default(html_1.default);
/**
 * Convert Markdown block to HTML
 *
 * @param {String} src (markdown)
 * @return {String} (html)
 */
function convertMdToHTMLBlock(src) {
    const content = markdown.toContent(src);
    const textHtml = html.toText(content);
    return textHtml;
}
/**
 * Convert Markdown inline to HTML
 *
 * @param {String} src (markdown)
 * @return {String} (html)
 */
function convertMdToHTMLInline(src) {
    const content = markdown.toInlineContent(src);
    const textHtml = html.toInlineText(content);
    return textHtml;
}
exports.default = {
    block: convertMdToHTMLBlock,
    inline: convertMdToHTMLInline,
};
